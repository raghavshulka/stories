import { StyleSheet, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


export default function SelectStory() {
  return (
    <SafeAreaView>
        <Text>Select Story</Text>+6++
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {},
});
